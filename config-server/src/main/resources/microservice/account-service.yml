spcloud:
   eureka:
     client:
       serviceUrl:
         defaultZone: http://localhost:8761/eureka/
   cassandra:
     contact:
       point: localhost
     port: 9042
     keyspace:
       name: spring_cloud
